services:
  postgres:
    image: postgres:16
    container_name: gotask-postgres
    restart: always
    environment:
      POSTGRES_DB: gotask_db
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
